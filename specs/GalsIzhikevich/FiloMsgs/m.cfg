CONSTANTS   InNeighbours <- IN_NEIGHBOURS
            Nodes <- NODES
            OutNeighbours <- OUT_NEIGHBOURS
            MaxTime <- MAX_TIME
            MaxMem  <- MAX_MEM

INIT    init

NEXT    next
